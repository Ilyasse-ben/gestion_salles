<div class="bg-white p-6 rounded shadow max-w-[800px] w-full">
  <h2 class="text-2xl font-bold mb-4">Mes réservations</h2>

  <table class="w-full border">
    <% reservations.forEach(r => { %>
      <tr class="border">
        <td class="p-2"><%= r.salle.nom %></td>
        <td class="p-2"><%= r.date.toISOString().slice(0,10) %></td>
        <td class="p-2"><%= r.heureDebut %> - <%= r.heureFin %></td>
      </tr>
    <% }) %>
  </table>
</div>
